language: python
python:
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install coveralls
  - python setup.py develop
# command to run tests
script:
  - coverage run --source elasticroute setup.py test
  
after_success:
  - coveralls